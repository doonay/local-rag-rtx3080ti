Это ключевой момент вашей архитектуры. Заранее определив DocumentChunk, вы обеспечиваете:

Независимость сервисов — можно менять embedder с BGE-M3 на OpenAI, а чанкер останется тем же

Версионирование — если формат меняется, это видно в коде

Тестируемость — можно мокать любой сервис

python
# common/schemas.py
from pydantic import BaseModel, Field
from typing import List, Optional, Dict, Any
from datetime import datetime
import uuid

class DocumentChunk(BaseModel):
    """Универсальный контракт для всех сервисов"""
    id: str = Field(default_factory=lambda: str(uuid.uuid4()))
    text: str
    metadata: Dict[str, Any]
    embedding: Optional[List[float]] = None
    
    class Config:
        json_encoders = {
            datetime: lambda v: v.isoformat()
        }